# DMFM テストシナリオ

このドキュメントでは、セットアップ直後の動作確認から、本番運用までのテストをステップバイステップで実施します。

---

## 📋 事前準備

```bash
# 環境変数を確認
echo "DISCORD_TOKEN: $DISCORD_TOKEN"
echo "MINECRAFT_ROOT: $MINECRAFT_ROOT"
echo "ADMIN_USER_ID: $ADMIN_USER_ID"
echo "APPROVAL_CHANNEL_ID: $APPROVAL_CHANNEL_ID"

# テスト用ディレクトリを作成
mkdir -p $MINECRAFT_ROOT/test
touch $MINECRAFT_ROOT/test/config.yml
echo "name: TestServer" > $MINECRAFT_ROOT/test/config.yml
```

---

## テスト 1: Bot の起動確認

### ステップ 1: Bot を起動

```bash
python dmfm_bot.py
```

### 期待される出力

```
╔════════════════════════════════════════════════════════╗
║  Discord Minecraft File Manager (DMFM) Bot            ║
║  ファイルパス安全性チェック: ✅ 有効                  ║
║  バックアップ世代: 1 世代（最新のみ）                 ║
╚════════════════════════════════════════════════════════╝

📁 ROOT_DIR: /home/minecraft/server
👤 Admin ID: 1218055725352484904
📢 Approval Channel: 123456789012345678

⏳ Bot 起動中...
✅ Bot が起動しました。ユーザー: DMFM-Bot#0000
✅ 4 個のスラッシュコマンドを同期しました
```

### チェックリスト

- [ ] ROOT_DIR が正しく表示されている
- [ ] Admin ID が正しい
- [ ] Approval Channel ID が設定されている（0 でない）
- [ ] 4 個のコマンドが同期されている

**もし失敗した場合**: [トラブルシューティング](#トラブルシューティング) を参照

---

## テスト 2: コマンドの表示確認

### ステップ 1: Discord で `/` をタイプ

- Minecraft サーバーのテスト用 Discord チャンネルを開く
- メッセージ欄に `/` とタイプ

### 期待される表示

以下の 4 つのコマンドが表示される：

```
/ls       📂 ディレクトリ内のファイル・フォルダ一覧を表示
/cd       📂 カレントディレクトリを変更
/edit     ✏️ 既存ファイルの編集
/new      ✨ 新規ファイルを作成
```

### チェックリスト

- [ ] 4 つのコマンドがすべて表示される
- [ ] 各コマンドに説明文が表示されている

**もし表示されない場合**:

```bash
# Discord クライアントをリロード
# Ctrl+R (Windows/Linux)
# Cmd+R (Mac)

# または Bot を再招待
# https://discord.com/developers/applications
```

---

## テスト 3: /ls コマンドの動作確認

### テスト 3-1: ROOT_DIR 直下の表示

**実行**:
```
/ls /
```

**期待される動作**:
- ROOT_DIR 内のすべてのフォルダとファイルが表示される
- フォルダには 📁、ファイルには 📄 マークが付く

**例**:
```
📂 /

📁 フォルダ:
  `plugins/`
  `config/`
  `test/`

📄 ファイル:
  `server.jar`
  `server.properties`

フォルダ: 3 個、ファイル: 2 個
```

### テスト 3-2: サブディレクトリの表示

**実行**:
```
/ls /test
```

**期待される動作**:
```
📂 /test

📄 ファイル:
  `config.yml`

フォルダ: 0 個、ファイル: 1 個
```

### テスト 3-3: 存在しないパスでのエラーハンドリング

**実行**:
```
/ls /nonexistent
```

**期待される動作**:
```
❌ エラー: パス `/nonexistent` が存在しません
```

### チェックリスト

- [ ] /ls / でルートが表示される
- [ ] /ls /test でサブディレクトリが表示される
- [ ] 存在しないパスでエラーが表示される

---

## テスト 4: /cd コマンドの動作確認

### テスト 4-1: カレントディレクトリの変更

**実行**:
```
/cd /test
```

**期待される動作**:
```
✅ カレントディレクトリを `/test` に変更しました
```

### テスト 4-2: 変更後の /ls を確認

**実行**:
```
/ls
```

**期待される動作**:
- `/test` 配下の内容が表示される
- `config.yml` が見える

### テスト 4-3: 無効なパスでのエラーハンドリング

**実行**:
```
/cd /invalid_path
```

**期待される動作**:
```
❌ エラー: `/invalid_path` が存在しません
```

### チェックリスト

- [ ] /cd で移動できる
- [ ] 移動後の /ls がそのディレクトリを表示
- [ ] 無効なパスでエラー表示

---

## テスト 5: /edit コマンド（正常系）

### ステップ 1: 編集用テストファイルを準備

```bash
cat > $MINECRAFT_ROOT/test/config.yml << 'EOF'
server:
  name: MyServer
  port: 25565
  motd: "Welcome!"
EOF
```

### ステップ 2: /edit コマンドを実行

**実行**:
```
/edit /test/config.yml
```

**期待される動作**:
- Bot が `config.yml` の現在の内容を表示
- 「このメッセージに返信でファイルをアップロード」というメッセージが表示

### ステップ 3: ファイルを編集してアップロード

1. ファイルをダウンロード（または表示されたテキストをコピー）
2. エディタで編集：

```yaml
server:
  name: MyServer Updated
  port: 25565
  motd: "Welcome to the new server!"
```

3. Bot のメッセージに **返信** する（右クリック → 返信）
4. 編集済みファイルをアップロード

### ステップ 4: 承認パネルが表示されることを確認

**承認チャンネルに以下が表示される**:

```
🔔 ファイル変更の承認が必要です

👤 操作者: @User
📁 ファイルパス: `/test/config.yml`
📋 差分:
  --- a/test/config.yml
  +++ b/test/config.yml
  @@ -1,4 +1,4 @@
   server:
  -  name: MyServer
  +  name: MyServer Updated
     port: 25565
  -  motd: "Welcome!"
  +  motd: "Welcome to the new server!"

[✅ 承認して反映] [❌ 却下]
```

### チェックリスト

- [ ] /edit でファイル内容が表示される
- [ ] 返信でファイルアップロードできる
- [ ] 承認パネルが承認チャンネルに表示される
- [ ] Diff が正しく表示されている

---

## テスト 6: /edit コマンド（承認・反映）

### ステップ 1: 承認ボタンをクリック

**承認チャンネルの「✅ 承認して反映」をクリック**

### 期待される動作

1. **承認チャンネルに成功メッセージが表示**:

```
✅ ファイルが反映されました

📁 `/test/config.yml`
👤 承認者: @Admin
⏰ 時刻: 2024-01-15 14:30:45
```

2. **編集したユーザーに DM が届く**:

```
✅ あなたの変更が承認され、`/test/config.yml` に反映されました。
```

3. **実ファイルが更新されている**:

```bash
cat $MINECRAFT_ROOT/test/config.yml
# 新しい内容が表示される

ls -la $MINECRAFT_ROOT/test/
# config.yml と config.yml.bak が見える
```

### チェックリスト

- [ ] 承認ボタンが反応する
- [ ] 成功メッセージが表示される
- [ ] ファイルが実際に更新されている
- [ ] .bak バックアップが作成されている

---

## テスト 7: /edit コマンド（却下）

### ステップ 1: 新しい /edit を実行

```
/edit /test/config.yml
```

### ステップ 2: 異なる内容をアップロード

```yaml
# 不適切な変更
server:
  broken: "This is wrong"
```

### ステップ 3: 承認パネルで「❌ 却下」をクリック

### 期待される動作

1. **承認チャンネルに却下メッセージが表示**:

```
❌ 変更が却下されました

📁 `/test/config.yml`
👤 却下者: @Admin
⏰ 時刻: 2024-01-15 14:35:22
```

2. **編集したユーザーに DM が届く**:

```
❌ あなたの変更が却下されました（`/test/config.yml`）
```

3. **ファイルは変更されていない**:

```bash
cat $MINECRAFT_ROOT/test/config.yml
# 前の内容（承認時の内容）が保持される
```

### チェックリスト

- [ ] 却下ボタンが反応する
- [ ] 却下メッセージが表示される
- [ ] ファイルが更新されていない
- [ ] ユーザーが DM で通知を受け取る

---

## テスト 8: /new コマンド（正常系）

### ステップ 1: /new コマンドを実行

```
/new /test/new_plugin.yml
```

**期待される動作**:

```
✨ 新規ファイル作成

📁 ファイルパス: `/test/new_plugin.yml`

ファイルの内容をアップロードしてください。
このメッセージにリプライで新しいファイルをアップロードしてください。
```

### ステップ 2: ファイルをアップロード

新しいファイル内容を含むファイルをアップロード：

```yaml
plugin:
  name: MyPlugin
  version: 1.0
  enabled: true
```

### ステップ 3: 承認パネルを確認

```
🔔 ファイル変更の承認が必要です

👤 操作者: @User
📁 ファイルパス: `/test/new_plugin.yml`
📋 差分:
  【新規ファイル作成】
  
  plugin:
    name: MyPlugin
    version: 1.0
    enabled: true
```

### ステップ 4: 管理者が承認

承認ボタンをクリック。

### 期待される動作

```bash
# ファイルが新規作成される
ls -la $MINECRAFT_ROOT/test/new_plugin.yml
# -rw-r--r-- 1 user group 95 Jan 15 14:40 /home/minecraft/server/test/new_plugin.yml

# 内容を確認
cat $MINECRAFT_ROOT/test/new_plugin.yml
# plugin:
#   name: MyPlugin
#   version: 1.0
#   enabled: true
```

### チェックリスト

- [ ] /new でウィザードが表示される
- [ ] ファイルをアップロードできる
- [ ] 承認パネルに「新規ファイル作成」と表示される
- [ ] 承認後、ファイルが作成されている
- [ ] .bak バックアップは作成されない（新規だから）

---

## テスト 9: セキュリティテスト

### テスト 9-1: ディレクトリトラバーサル対策

**実行**:
```
/edit ../../../etc/passwd
```

**期待される結果**:
```
❌ エラー: パスが無効です（ROOT_DIR 外へのアクセスは不可）
```

**チェックリスト**:
- [ ] エラーが表示される
- [ ] システムファイルにアクセスできない

### テスト 9-2: 絶対パスの外部アクセス対策

**実行**:
```
/edit /etc/passwd
```

**期待される結果**:
```
❌ エラー: パスが無効です（ROOT_DIR 外へのアクセスは不可）
```

### テスト 9-3: 管理者でない者の承認ボタン操作

**テスト手順**:

1. Bot のメッセージで /edit を実行
2. 承認チャンネルの承認ボタンを、**管理者以外のユーザーがクリック**

**期待される結果**:

```
❌ エラー: この操作は管理者のみ実行できます。
```

**チェックリスト**:
- [ ] 管理者でない者は操作できない
- [ ] エラーメッセージが表示される（他者には見えない）

### テスト 9-4: バックアップの上書き確認

**テスト手順**:

1. ファイルを編集・承認（.bak が作成される）
2. 同じファイルを再度編集・承認

**確認**:

```bash
ls -l $MINECRAFT_ROOT/test/config.yml*
# config.yml (新しい内容)
# config.yml.bak (2番目の編集内容)

# 最初の編集内容を確認
cat $MINECRAFT_ROOT/test/config.yml.bak
# 2番目の編集内容が表示される（最初の編集内容ではない）
```

**チェックリスト**:
- [ ] .bak は最新 1 世代のみ保持される
- [ ] 3 世代以上前は保持されない

---

## テスト 10: 非管理者ユーザーの権限確認

### テスト 10-1: /ls コマンドは誰でも使用可能

**テスト**:
- 管理者以外のユーザーで `/ls` を実行

**期待される結果**:
```
✅ ファイル一覧が表示される
```

### テスト 10-2: /edit と /new は誰でも提案可能

**テスト**:
- 管理者以外のユーザーで `/edit /test/config.yml` を実行
- ファイルをアップロード

**期待される結果**:
```
✅ 変更提案が送信されました。管理者の承認を待ってください。
```

### テスト 10-3: 承認・却下は管理者のみ

**テスト**:
- 管理者以外のユーザーが承認ボタンをクリック

**期待される結果**:
```
❌ エラー: この操作は管理者のみ実行できます。
```

**チェックリスト**:
- [ ] Read/List (ls) は全ユーザー可能
- [ ] Propose (edit/new) は全ユーザー可能
- [ ] Approve/Reject は管理者のみ

---

## テスト 11: 本番環境への移行チェックリスト

### Bot 構成

- [ ] DISCORD_TOKEN が `.env` ファイルで安全に管理されている
- [ ] TOKEN がソースコードに含まれていない
- [ ] `.env` ファイルが `.gitignore` に追加されている
- [ ] systemd または pm2 で自動起動が設定されている

### ファイルシステム

- [ ] ROOT_DIR が正しく設定されている
- [ ] Bot 実行ユーザーが ROOT_DIR への書き込み権限を持っている
- [ ] ディスク容量が十分である（ df -h）

### Discord 設定

- [ ] ADMIN_USER_ID が正しい
- [ ] APPROVAL_CHANNEL_ID が正しい（0 でない）
- [ ] Bot が該当チャンネルへのメッセージ送信権限を持っている
- [ ] Bot が該当チャンネルでメッセージを削除しないようにしている

### セキュリティ

- [ ] ディレクトリトラバーサル対策が機能していることを確認
- [ ] 管理者権限の確認が厳格に行われていることを確認
- [ ] バックアップが正常に作成されることを確認

### ログ・モニタリング

- [ ] Bot のログ出力先を決定している
- [ ] ログローテーション設定が完了している
- [ ] 定期的にログを確認する手段を用意している

---

## トラブルシューティング

### 問題: Bot が起動しない

```bash
python dmfm_bot.py 2>&1 | head -50
```

**確認ポイント**:
- [ ] DISCORD_TOKEN が有効か
- [ ] discord.py がインストール済みか
- [ ] Python バージョンは 3.8 以上か

### 問題: コマンドが表示されない

```bash
# Bot ログで確認
tail -f /var/log/dmfm-bot.log | grep -i command
```

**確認ポイント**:
- [ ] Bot が正常に起動しているか
- [ ] Discord クライアントをリロードしたか
- [ ] 権限に `applications.commands` が含まれているか

### 問題: ファイルがアップロードされないと認識される

**ユーザーへのアドバイス**:
- [ ] 必ず「リプライ」で返信する（右クリック → 返信）
- [ ] リプライ以外のメッセージは無視される
- [ ] ファイルは UTF-8 でエンコードされているか確認

### 問題: ファイルが上書きされない

```bash
# 権限を確認
ls -l /home/minecraft/server/test/
# Bot ユーザーが w 権限を持っているか

# ファイルがロックされていないか確認
lsof /home/minecraft/server/test/config.yml
```

---

## 次のステップ

テストがすべてパスしたら：

1. **本番チャンネルで運用開始**
   - テスト用チャンネルから本番チャンネルに移行

2. **定期メンテナンスの予定化**
   - バックアップ確認（週 1 回）
   - ログレビュー（月 1 回）

3. **フィードバック収集**
   - ユーザーからの改善要望
   - エラーログの定期確認

---

## 完了チェック

```bash
# すべてのテストを完了したら：
echo "✅ DMFM テストスイート完了"
echo "✅ 本番運用開始準備完了"
```

Happy Testing! 🚀
